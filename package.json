{
  "name": "the-great-nizam-unified",
  "version": "1.0.0",
  "description": "Unified Great Nizam POS Deployment",
  "scripts": {
    "install-all": "npm install --prefix backend && npm install --prefix frontend",
    "fix": "node fix-files.js",
    "build": "npm run fix && npm run install-all && npm run build --prefix frontend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.21.0"
  }
}
